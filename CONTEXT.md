# 链路预算计算器上下文

本项目用于展示和编辑链路预算参数矩阵，并与 Excel 交换表格数据。当前阶段先聚焦场景生命周期、输入编辑和自由空间路损模型的覆盖距离计算，后续再扩展完整链路预算能力。

## 语言

**参数名称**：链路预算矩阵中每一行参数的名称，例如载波频率、发射功率。它属于只读结构列。

**单位**：参数的计量单位，例如 GHz、dBm、dB、km。第一版将单位并入最左侧参数名称显示，不单独占用表格列。

**路损**：无线链路传播造成的信号功率衰减量，以 dB 表示；第一版的“路损值”由用户输入，并作为模型使用的总路损值。路损值允许暂时为空，非空时必须大于 0。

**路损模型**：描述路损与频率、距离、环境等条件之间关系的规则，既可以计算路损，也可以根据给定路损反推出距离。

**自由空间路损模型**：在理想自由空间传播假设下，根据频率和距离描述路损关系的首个模型；初期每个场景输入载波频率和给定路损，输出覆盖距离。
**自由空间路损计算**：使用 GHz、dB 和 km 作为初期界面单位，根据标准自由空间路损公式计算或反解路损与距离关系。

**覆盖距离**：在给定总路损值、路损模型及其所需条件下，满足模型路损不超过该上限的最大链路距离；它是由计算引擎生成的只读结果。

**速率需求**：链路必须达到的目标数据传输速率，后续用于计算最大允许路损。

**最大允许路损**：在输入参数和速率需求约束下仍能满足目标的最高路损，后续作为计算结果用于推导覆盖距离。

**模型诊断**：路损模型在验证或计算时返回的结构化问题，说明输入为何不在模型定义范围内或无法产生结果；界面将其转换为用户可读提示。

**正向路损计算**：根据路损模型、传播距离及模型所需条件计算路损的过程。

**路损反解**：根据路损模型、载波频率和最大允许路损计算最大覆盖距离的过程。

**只读结构列**：表格最左侧的“参数名称”列，不允许用户直接编辑。分组名称位于表格左侧的独立分组栏，不属于表格单元格。

**场景值**：某个输入参数在一个链路场景列中的具体值，是当前阶段允许用户编辑和与 Excel 交换的内容；计算结果不属于场景输入。
**场景**：一个独立链路预算配置及其计算结果的集合，以矩阵列呈现；每个场景拥有独立的路损模型、模型参数和其他输入参数。

**场景输入**：属于单个场景、由用户独立配置并作为计算依据的参数集合。
**场景默认模板**：新增场景使用的一组初始输入值；第一版直接使用固定默认值，不从其他场景复制。
**空场景集合**：所有场景都被删除后的状态；此时没有可计算的场景，也不产生覆盖距离或其他结果。
**场景列操作**：场景标题旁显示小型 × 按钮，点击后立即删除对应场景列；双击场景标题进入文字编辑，Enter 或失焦提交，Esc 取消；场景集合允许为空，第一版不提供撤销。
**场景标题**：场景列顶部唯一显示的名称；第一版不显示频率、距离等副标题。单击标题保留表格选列行为，双击标题进入原位编辑。

**计算输入**：场景中由用户编辑、作为路损模型计算依据的参数，包括路损模型、载波频率、路损值以及当前界面保留的其他链路预算参数。不同场景的计算输入相互独立。
**计算结果**：由路损模型根据场景输入生成的只读值，统一放在表格底部的“计算结果”分组中。第一版结果为覆盖距离，后续允许增加其他结果行。
**自动计算**：页面首次打开时计算默认输入；输入编辑提交或一整块 Excel 粘贴完成后，重新计算受影响的场景。无效输入时不保留过期结果。

**Excel 粘贴**：将 Excel 中以 Tab 分列、换行分行的文本，从当前选中范围的左上角开始填充到表格。它不识别或跳过表头；如果左上角不是可更新的场景值单元格，则整次粘贴不执行。

**可更新场景值单元格**：输入参数行与场景值列交叉形成的、用于承载参数值的单元格；“路损模型”使用下拉选择，其他输入先使用文本输入。计算结果行和最左侧参数名称列不属于可更新场景值单元格。

**静态参数矩阵**：原型阶段保留现有链路预算参数行，加入“路损模型”和“路损值”输入行，移除“环境附加损耗”和“静态评估”行；场景列由场景集合动态决定。后续模型可以根据自身定义增加或隐藏参数行。

**数值编辑**：对可更新输入单元格双击后使用普通文本输入框编辑；Enter 或失焦提交，Esc 取消。输入值先保留用户原始文本，由计算层解析。

**区域粘贴边界**：Excel 粘贴的起始选中单元格必须是可更新场景值单元格，否则整次粘贴不执行；合法开始后保持源数据的行列位置，只更新可更新输入单元格，只读结果位置跳过，超出范围的数据忽略。不自动识别或跳过表头。

**Excel 复制**：复制选中的表格范围时，以纯文本 Tab/换行格式输出；只读单元格（包括覆盖距离结果）可以复制，分组名称不复制。场景标题被复制时使用最新名称；只有列标题也被明确选中时，才在数据前加入对应表头。

**内容持久化**：将用户编辑或 Excel 粘贴后的场景值保存到页面刷新之后仍可恢复的机制。本阶段不提供内容持久化，刷新后恢复默认内容。

**报告布局**：本阶段唯一保留的静态参数矩阵展示形式；原型中的其他布局和布局切换不属于本阶段范围。

**可编辑范围**：除最左侧参数名称列、左侧分组栏和计算结果行外，场景列中的输入单元格均可编辑；路损模型使用下拉选择。

**编辑提交**：输入单元格通过双击进入编辑，Enter 或失焦提交，Esc 取消；提交后自动重新计算对应场景。空值可以保留，模型负责返回缺少输入的诊断。

**固定参数矩阵**：本阶段保留原型现有参数行，不提供参数行的新增或删除；场景列不属于固定结构，允许按场景生命周期规则变化。
